"use strict";
var age = 32; // number variable
var name1 = "John"; // string variable
var isUpdated = true; // Boolean variable
function display1(id, name) {
    console.log("Id = " + id + ", Name = " + name);
}
function display(id, name) {
    return ("Id = " + id + ", Name = " + name);
}
function display2(id, name) {
    console.log("Id = " + id + ", Name = " + name);
}
let employeejs = {
    name: "string",
    roll: 1
};
var employee;
employee: employee = {
    id: 100,
    name: "John"
};
